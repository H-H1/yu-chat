const content = `# Harness Engineering 实战：从一句话到音乐制作软件

> Anthropic 刚发布的实战案例：用 Harness Engineering 的方法，让 Claude 自主写了 4 个小时代码，从一句话 prompt 生成了一个能用的浏览器端音乐制作软件。不是 demo，不是 toy project，是一个有编曲视图、混音器、音轨编辑，还内置 AI 助手的完整应用。

---

## 先看一个数据对比

同一句 prompt：创建一个 2D 复古游戏制作器。

| | 单 Agent | 多 Agent Harness |
| --- | --- | --- |
| 耗时 | 20 分钟 | 6 小时 |
| 成本 | $9 | $200 |
| 结果 | 界面看着还行，一点就碎，核心功能坏掉 | 核心功能可用，自动规划出 16 个功能、10 个迭代周期 |

单 Agent 版本：角色放到关卡里什么输入都没有响应，代码里实体定义和游戏运行时的连线是断的，而且界面上完全看不出来哪里出了问题。

Harness 版本：角色能移动、能跳跃，还包含精灵、动画系统、行为模板、音效，甚至还有 AI 辅助的关卡生成器。

> **同一个模型，同一句 prompt，产出天差地别。差的不是智能，是智能周围的系统。**

---

## 核心架构：GAN 思想的迁移

Anthropic 借鉴了深度学习里经典的 GAN（生成对抗网络）结构：

- **生成器**：负责创造
- **判别器**：负责挑刺
- 两者对抗博弈，互相逼着对方进步

他们把这个思路搬到了 Agent 架构上，设计了一个 **3 Agent 系统**：

\`\`\`
规划器 → 生成器 ↔ 评估器
              ↑_______↓（对抗循环）
\`\`\`

每个 Agent 解决一个明确的失败模式。

---

## 规划器：只给方向，不给路径

大多数人用 Agent 写代码，上来就是一句 prompt 直接开干。Anthropic 发现这样做，Agent 会严重低估项目的范围，做出来的东西功能很单薄。

所以他们加了一个规划器 Agent，专门负责把一句话 prompt 扩展成一份完整的产品规格说明。

**反直觉的设计决策**：规划器只写高层产品设计，**故意不写技术实现细节**。

为什么？因为如果规划器在前期把某个技术细节写错了，这个错误会一路传导到后面所有的实现环节，越错越离谱。不如只约束要做什么，让后面的 Agent 自己决定怎么做。

> **当你设计多 Agent 系统时，上游 Agent 的输出力度要非常克制，给方向但不给路径。**

---

## 生成器：上下文焦虑与重置

生成器就是真正写代码的 Agent，按迭代周期工作，一次只做一个功能，每个迭代周期结束后自评一次，再交给评估器。

### 第一个重大失败模式：上下文焦虑

当 Agent 持续工作，上下文窗口越填越满的时候，有些模型会开始"慌"——觉得自己快到上下文上限了，然后开始草草收工，提前结束任务。

注意：**不是真的到了上限，而是模型自己以为快到了。**

这个问题用常规的上下文压缩解决不了。压缩是把前面的对话摘要一下，让 Agent 在缩短的历史上继续工作。但问题是，Agent 虽然拿到了更短的上下文，心理上的焦虑并没有消失，它仍然觉得自己快到极限了。

### 解法：上下文重置

不是压缩，而是**彻底清空**。启动一个全新的 Agent，然后通过一个结构化的交接文档，把前一个 Agent 的状态和下一步计划传递过去。给模型一块干净的白板。

代价是增加了编排复杂度和延迟，但效果显著。

> **后续发现**：当 Anthropic 发布了更强的 Opus 模型之后，上下文焦虑这个问题基本消失了。这意味着上下文重置这个组件可以被拆掉了——这正是 Harness 模块化设计的价值所在。

---

## 评估器：最关键也最难做好的部分

### 第二个重大失败模式：自我表扬

Agent 在评价自己的工作时，倾向于自信地表扬自己。即使产出质量明显平庸，它也会说"做的不错"。

这个问题在主观任务上尤其严重——前端设计好不好看，本身就是一个主观判断，没有二元的对错。Agent 自评的时候几乎永远给自己高分。

但即使是在有客观标准的编程任务上，Agent 也会犯同样的错误：发现了一个 bug，然后说服自己这不是大问题，然后放行了。

### 解法：生成与评估彻底分离

让另一个独立的 Agent 来当裁判。但注意，**仅仅分离是不够的**，因为评估器本身也是一个 LLM，它天然对 LLM 生成的内容宽容。

关键洞察：**调用一个独立的评估器让它变严格，比让生成器学会自我批判要容易得多。** 一旦严格的外部反馈存在了，生成器就有了具体的改进目标。

### 怎么让评估器真正严格起来

**第一步：把主观的"好不好"变成可打分的具体维度**

Anthropic 定义了四个评分标准：

| 维度 | 说明 | 权重 |
| --- | --- | --- |
| 设计质量 | 颜色、排版、布局是否形成统一的整体感 | 高 |
| 原创性 | 有没有定制化的设计决策，还是千篇一律的模板套路 | 高 |
| 工艺 | 技术执行力，兼容性、一致性、对比度等基本功 | 低 |
| 功能性 | 用户能不能顺利完成任务 | 低 |

工艺和功能性权重低，因为 Claude 默认就做的不错。设计质量和原创性权重高，因为这是模型的短板。

而且评分标准里明确写了：**要惩罚那些一眼就能看出是 AI 生成的套路**，比如紫色渐变配白色卡片。

**第二步：用 few-shot 示例加详细的评分分解来校准评估器**

评估器不是看截图打分的，它通过 Playwright 实际操作运行中的应用，点击导航、截图仔细研究，然后才输出评估。每一轮反馈都回传给生成器，生成器要么继续优化当前方向，要么整体推翻重来。

### 对抗式反馈循环的力量

一个惊艳的例子：让模型生成一个荷兰艺术博物馆的网站。

前九轮迭代，模型一直在做一个深色主题的落地页，视觉上很精致，但在预期范围内。

到第十轮，模型突然推翻了整个方案，把网站重新想象成一个**空间体验**——一个用 CSS 透视渲染的 3D 房间，棋盘格地板，画作自由悬挂在墙上，通过门廊在画廊之间导航。

这种创造性的跳跃在单次生成中从来没见过。

> 不是让模型一次做到最好，而是让它在严格的外部反馈下，一轮一轮地逼近好的方向，甚至逼出你预料之外的方向。

---

## 迭代合同机制

在代码编写场景下，生成器和评估器在每个迭代周期开始之前，会先**协商一份合同**：

- 明确定义这个迭代周期做到什么程度算完成
- 包括具体要实现的功能和可测试的验收标准
- 双方反复讨论，直到达成一致
- 生成器按合同构建，评估器按合同验收

**为什么要这样做？**

规划器的产品规格说明是故意写的高层的，不会细到告诉你某个按钮应该怎么实现。迭代合同就是在高层规格和可测试的实现之间搭了一座桥。

**通信方式**：Agent 之间不是通过对话来沟通的，而是通过文件。一个 Agent 写一个文件，另一个 Agent 读这个文件然后回应。简洁、可追溯，不会污染各自的上下文。

---

## 评估器的实际严格程度

文章给了几个实际例子：

**关卡编辑器的矩形填充工具**：评估器发现它只在拖拽起点和终点放置了瓷砖，而不是填满整个矩形区域。精确指出了是哪个函数没有在 mouseup 事件上被正确触发。

**FastAPI 的路由问题**：评估器发现 reorder 这个路径被定义在了带参数的路由后面，导致 FastAPI 把 reorder 这个字符串当成了整数类型的 frame_id 去解析，返回了 422 错误。

这种力度的 QA 是真正在用的层面上找问题，而不是走过场。

**但 Anthropic 也非常坦诚**：让评估器达到这个水平花了大量功夫。Claude 开箱即用的时候是一个很差的 QA Agent。他们的调优方法是一个人工迭代循环：读评估器的日志 → 找到它的判断和你的判断不一致的地方 → 改 prompt 再跑 → 再看日志再改，反复多轮之后，评估器才开始输出合理的判断。

---

## 最有战略价值的认知：Harness 是可拆卸的

当 Anthropic 发布了更强的 Opus 模型之后，他们做了一件很重要的事：**回过头去重新审视整个 Harness，看哪些组件还有存在的必要。**

他们提出了一个原则：

> **Harness 中的每一个组件本质上都编码了一个假设——模型自己做不好这件事。这些假设需要反复被压力测试，因为它们可能一开始就是错的，也可能随着模型进步而过时。**

### 拆卸方法论

他们一开始想大刀阔斧地简化，直接砍掉一半组件，结果失败了——产出质量明显下降，而且搞不清楚是哪个组件的缺失导致的。

后来换了一个更稳妥的方法：**每次只移除一个组件，观察对最终结果的影响。**

最终他们成功移除了整个迭代周期结构。因为 Opus 的持续工作能力大幅提升，生成器可以连续编码两个多小时而不丧失连贯性，不再需要把工作切成小块。评估器也从逐周期评审改成了最后统一评审一次。

**但评估器本身没有被移除**，因为在模型能力的边界上，评估器仍然能抓到生成器遗漏的功能和 bug。它的价值变成了动态的：任务越接近模型的能力极限，评估器的价值就越大。

### 简化后的成果

简化之后的 Harness 跑了一个浏览器端的数字音频工作站：

- 耗时：4 小时
- 成本：$125
- 产出：有编曲视图、混音器、多音轨编辑的完整应用
- 内置 AI 助手：可以通过 prompt 设置节拍、编写旋律、构建鼓轨、调整混音、添加混响

从一句话 prompt 到一个能用的音乐制作软件，全程无人干预。

---

## 核心认知总结

**1. 永远不要让 Agent 自己评价自己的工作**

生成和评估必须分离，然后花时间把评估器调严格。

**2. 主观质量是可以被打分的**

定义具体的评分维度，用 few-shot 校准，按短板加权，你就有了一个可以驱动迭代的反馈信号。

**3. 上游规划要克制**

只给方向不给路径，避免错误级联。

**4. Harness 的每一个组件都应该是可拆卸的**

模型在进步，今天需要的脚手架明天可能变成瓶颈。保持模块化，定期审视，每次只动一个变量。

**5. Harness 的设计空间不会随模型进步而缩小**

模型能力的边界往外推了，Harness 的设计空间也跟着往外推。

> **Agent 的性能天花板不取决于模型能做什么，而取决于你在模型周围搭建了什么样的系统。模型进步一个台阶，Harness 跟着进化一个台阶，两者叠加才是真正的前沿。**
`;

export default content;
