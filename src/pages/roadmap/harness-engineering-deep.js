const content = `# Harness Engineering 全景：概念、实践与争议

> 2026 年上半年 AI 工程圈升温速度最快的概念。如果你现在还停留在"怎么写一条更好的 prompt"这个层面去做 Agent，这篇文章你一定要看完。

---

## 什么是 Harness Engineering？

**Harness** 这个词的原始含义是马具——缰绳、马鞍、辔头，一整套控制马匹的装备。这个比喻是刻意的：

- 马 = 模型，强大快速，但自己不知道该往哪儿跑
- 骑手 = 人类工程师，提供方向
- 马具 = Harness，把这匹马的原始能力导向有用的工作

翻译成技术语言就是一句话：

> **Harness Engineering 不是在教模型怎么回答，而是在设计模型怎么工作。**

它处理的是模型外部那一整层东西：任务怎么拆解、上下文怎么管理、工具怎么编排、权限怎么设定、状态怎么交接、做完了怎么验证、失败了怎么恢复、什么时候该把控制权交回给人类。

这不是一条 prompt 能搞定的事情，这是一个完整的运行系统设计问题。

---

## 概念的结晶时刻

- **2025 年 11 月**：Anthropic 发布工程博客 *Effective Harnesses for Long-Running Agents*，已经把 Claude Agent SDK 定义为通用型 Agent Harness。**Anthropic 是实践在先。**
- **2026 年 2 月 5 日**：Hashicorp 联合创始人 Mitchell Hashimoto 发了一篇博客，说了一句极简洁的话：*每当你发现 Agent 犯了一个错误，你就花时间工程化一个解决方案，让它永远不再犯同样的错。* **Mitchell 是命名者。**
- **一周后**：OpenAI 在官方博客直接发了一篇文章，标题就叫 *Harness Engineering*，正式把这个术语推到台前。**OpenAI 是推广者。**

---

## 三层概念的包含关系

\`\`\`
Harness Engineering（让模型在什么运行机制里干活）
    └── Context Engineering（给模型看什么）
            └── Prompt Engineering（怎么说）
\`\`\`

| 层级 | 主导时期 | 核心问题 |
| --- | --- | --- |
| Prompt Engineering | 2022–2024 | 怎么措辞？怎么给 few-shot 示例？ |
| Context Engineering | 2025 | 让模型看到什么？RAG、记忆注入、对话历史管理 |
| Harness Engineering | 2026 起 | 模型能用什么工具？拥有什么权限？失败了怎么重试？ |

Karpathy 有一个精彩的类比：把大语言模型看成 CPU，上下文窗口看成 RAM，Context Engineering 就是操作系统层面管理工作内存的技术。而 Harness 是整辆车——方向盘、刹车、车道边界、维护计划、警示灯，以及确保车门不会在高速公路上脱落的所有工程设计。

---

## 为什么偏偏 2025 底到 2026 初爆发？

**原因一：模型能力抬高后，系统设计变成了主要差异来源**

模型够强了，但光强没用，你得给它一个好的工作环境，它才能把能力发挥出来。

**原因二：长任务暴露了裸模型的系统性缺陷**

即使是 GPT-4.5，在跨多个上下文窗口运行时，如果只给一个高层级指令，它依然造不出生产质量的 Web 应用。失败模式非常典型：要么试图一口气完成所有事情导致上下文耗尽，要么下一个 session 看到一部分进展就提前宣布完成而不验证。换一个更强的模型不会自动消失，必须靠 Harness 层面的机制来治理。

**原因三：串联失败的数学**

假设一个多步 Agent 流水线中每一步的成功率是 95%，听起来很高了，但串联 20 步之后：

\`\`\`
0.95^20 ≈ 36%
\`\`\`

端到端的任务完成率只剩 36%。这就是为什么团队报告说 Agent 95% 的时间都在正常工作，但真实任务上仍然有三分之一的失败率。这个问题不是靠更聪明的模型能解决的，必须靠系统层面的验证。

**原因四：模型趋于商品化**

GPT 系列、Claude 系列、Gemini 系列在核心能力上的差距在缩小。当模型本身不再是差异化因素的时候，围绕模型的系统设计——也就是 Harness——就成了新的竞争壁垒。

> 旧护城河是模型质量，新护城河是 Harness 质量。

---

## 头部公司怎么做 Harness？

### Anthropic：从双 Agent 到三 Agent 的演进

**第一版：双 Agent 架构（2025 年 11 月）**

- **初始化 Agent**：只在第一个 session 运行，负责建立环境、创建初始化脚本、写入进度日志文件、建立 Git 基线，然后把用户的高层级指令扩展成数百条具体的可测试的功能需求清单（JSON 格式存下来）
- **编码 Agent**：在后续 session 中逐个功能推进，每次启动先确认当前位置，读进度文件，审查功能清单，跑已有测试

关键设计思想：**外部制品成为 Agent 记忆**——进度文件、Git 历史、结构化需求清单跨 session 持久化。每个 Agent session 在动手之前，先从这些制品重建上下文。

有趣的技术细节：这两个 Agent 其实并不是真正独立的 Agent，它们共享相同的系统提示、工具集和整体 Harness，唯一区别只是初始用户提示不同。**仅通过提示工程就能在单一 Harness 内创造专门化行为。**

**第二版：三 Agent 架构（2026 年 3 月）**

\`\`\`
Planner（扩展需求）→ Generator（实现）↔ Evaluator（验证打分）
                                    ↑_________________↓
\`\`\`

最有意思的发现：**评估器分离**。

Anthropic 发现，当你让模型评估自己的工作时，它会倾向于自信地表扬自己的作品，即使在人类看来质量明显平庸。这不是哪个模型的问题，这是自评估的系统性缺陷。

结论：工程化一个独立的严格评估器 Agent，远比教会生成器 Agent 自我批评要容易得多。

**模型迭代带来的 Harness 变化**：早期 Harness 用的是 Sonnet 4.5，这个模型有明显的上下文焦虑倾向，随着上下文增长模型会变得不稳定，所以 Harness 里设计了上下文重置机制。但换成 Opus 4.5 之后，模型自行消除了这个行为，上下文重置机制就变得不再必要了。

> **Harness 不是越复杂越好，它必须跟模型当前的能力边界相匹配。模型强了，有些 Harness 模块反而应该撤掉。**

---

### OpenAI：百万行代码的工程实践

2025 年 8 月开始，一个最初只有 3 人、后来扩展到 7 人的工程团队，用 GPT-5 驱动的 Codex Agent 在约五个月里：

- 生成了约 **100 万行代码**
- 合并了约 **1500 个 PR**
- 构建了一个有内部日活用户和外部 Alpha 测试者的生产级产品
- 团队人均日吞吐量约 **3.5 个 PR**，而且随着团队扩大，吞吐量反而上升了

他们给自己加了一个极端约束：**禁止手写代码**，所有应用逻辑、测试、CI、文档、可观测性和内部工具全部由 Codex 生成。

**三大支柱**（Martin Fowler 网站上的分析归纳）：

1. **Context Engineering**：持续增强代码库中的知识库，加上 Agent 对可观测性数据和浏览器的动态访问
2. **架构约束**：不只是靠 Agent 自觉遵守，而是用确定性的自定义 linter 和结构测试来执行
3. **垃圾回收**：定期运行后台 Agent 扫描文档不一致和架构违规，对抗系统的熵增

核心模式：**当 Agent 遇到困难时，不要更努力地尝试，而是反问缺少什么能力，怎么让这个能力对 Agent 来说既可读又可执行，然后让 Codex 自己编写修复代码**——形成 Harness 自我改进的闭环。

⚠️ **诚实度提醒**：OpenAI 在呈现这个案例时有既得利益。而且那篇文章标题虽然叫 *Harness Engineering*，但正文里 harness 这个词只出现了一次，标题很可能是受 Mitchell 那篇博客启发后加上去的。该学的学，该存疑的也得存疑。

---

### Google DeepMind：AlphaProof 的三组件架构

2026 年 2 月，DeepMind 发布了 AlphaProof，一个面向数学研究的自主 Agent。核心架构：

\`\`\`
Generator（提出候选解法）→ Verifier（检查逻辑缺陷）→ Reviser（修正错误）
        ↑_________________________________________________↓（循环迭代）
\`\`\`

注意到了吗？Generator → Verify → Revise，这跟 Anthropic 的 Planner → Generator → Evaluator 高度对应。**两家公司独立走到了同一个设计模式上，这不是巧合。**

这说明**生成-评估分离正在成为 Agent Harness 设计的行业共识**。

成果：在 IMO Proof Bench Advanced 上达到 95.1% 的准确率，自主解决了 Idol 猜想数据库中 4 个此前未被解决的开放问题。但也有批评性报道指出，它在更广泛的问题集上错了 68.5%——这恰好说明了 Harness 的特点：在特定场景下可以非常强大，但泛化能力仍然受限于 Harness 的设计边界。

---

### Vercel：约束反而提升性能

Vercel 最初给 Agent 配了一个非常全面的工具库，结果效果很差——Agent 变得困惑，进行冗余调用，执行不必要的步骤。

然后 Vercel 做了一件看起来倒退的事：**移除了 80% 的工具**。

结果：更少的步骤、更少的 token 消耗、更快的响应、更高的成功率。

> **约束 Agent 的解决空间，反而能提升它的表现。**

这个洞察可以提炼到更深的层次：为了获得更多 AI 自主性，运行时环境反而需要更多约束，而非更少护栏。这跟传统软件开发里"给工程师更多自由度"的理念完全相反。

---

### Stripe：与人类工程师相同的上下文

Stripe 的 Agent 叫 Minerva，运行在隔离的、预热好的 dev box 环境里，跟人类工程师用的开发环境一样，但与生产环境和互联网隔离。Agent 通过 MCP 服务器访问超过 400 个内部工具。

核心理念：**Agent 需要跟人类工程师一模一样的上下文和工具，而不是后期拼凑的集成方案。**

---

## 成熟 Harness 的六大模块

综合 Anthropic、OpenAI、Google DeepMind、Stripe 的实践：

### 模块一：上下文工程与知识管理

- **项目指令文件**：agents.md、claude.md 等，Agent 启动时读取的文档
- **动态上下文注入**：从日志、指标、追踪信息中获取实时信息
- **上下文隔离**：用子 Agent 作为上下文防火墙，让不同子任务在隔离的上下文窗口里运行
- **上下文压缩**：随着窗口被填满，自动丢弃或摘要无关信息

> OpenAI 的精辟观察：从 Agent 的角度看，它在运行时无法访问的任何东西都等同于不存在。所以越来越多的知识需要被推送到代码库内部，成为版本化的制品。

### 模块二：工具编排与权限设计

精心策划工具集，移除冗余选项。包括 MCP 协议集成、文件系统访问管理、沙箱隔离等。

### 模块三：验证机制与硬约束

这是 Harness 区别于简单 scaffolding 的核心特征：

- **确定性约束**：自定义 linter、结构测试、pre-commit hooks，不依赖 LLM 判断的硬性规则
- **生成-评估分离**：独立的评估器 Agent
- **自动审查循环**：OpenAI 的系统里，Codex 会在本地审查自己的更改，其他 Agent 审查回应所有反馈，在循环里迭代直到所有审查者都满意

### 模块四：状态管理与记忆持续性

大语言模型是无状态的，每个新 session 从零开始。解决办法：

- 外部化记忆（进度追踪文件、结构化功能清单）
- 增量 Git 提交
- 检查点与恢复机制

### 模块五：可观测性与反馈闭环

- 执行追踪、质量分级、异常检测
- **反馈归因**：把 Agent 在生产中的失败模式追溯到 Harness 的具体缺陷，驱动持续改进

### 模块六：人类接管与生命周期管理

- 在关键决策点暂停（要删数据库、要扣费、要发客户邮件，必须让人类确认）
- 升级路径、失败熔断
- 完整的生命周期钩子

---

## 新瓶装旧酒，还是真正的新方法论？

**确实不是新发明的部分**：

Test harness 在软件工程里已经有几十年历史。CI/CD 管道、任务分解与编排、沙箱隔离、可观测性——这些都是成熟的工程实践。

**但它确实有新的方法论贡献**：

**1. 约束对象发生了根本变化**

传统软件工程约束的是确定性代码执行，而 Harness Engineering 约束的是概率性推理系统。这要求在验证、重试、恢复等方面采用根本不同的设计模式。

**2. 约束以提升这个反直觉原则**

Vercel 的案例证明了约束 Agent 的解决空间——减少工具、限定模式、强制架构边界——反而提升了它的生产力和可靠性。这跟给人类工程师更多自由度的思路是相反的。

**3. 生成-评估分离模式**

Anthropic 的 Planner-Generator-Evaluator，Google DeepMind 的 Generator-Verifier-Reviser，独立收敛到了同一个模式。这已经不是个别公司的偏好，而是工程实践倒逼出来的结构性结论。

**4. 代码库本身成为 Harness 的一部分**

代码结构、命名约定、模块边界不仅服务于人类可读性，更服务于 Agent 的可推理性。OpenAI 的代码库甚至首先为 Codex 的可读性优化，而不是人类的阅读偏好。

> Harness Engineering 更准确的定位是：在 Agent 生产化的压力下，将多个成熟工程领域的实践重新组合、调适，并补充针对概率性推理系统的新模式，形成了一个统一的方法论。就像 DevOps 重组了开发与运维实践，Harness Engineering 正在重组 Agent 系统的构建与运维实践。

---

## 风险与局限

**风险一：概念膨胀**

Harness Engineering 正在变成一个什么都能往里装的筐。当一个术语从 agents.md 文件到完整的生产运维系统都能涵盖时，它的精确性和实用性就会被稀释。

**风险二：过度工程化**

OpenAI 自己都强调 Harness 必须是可撕裂的。随着模型变强，之前需要复杂 Harness 来补偿的问题可能被模型直接解决。Anthropic 已经给出了实证：Opus 4.5 自己消除了 Sonnet 4.5 的上下文焦虑行为，早期 Harness 里的上下文重置机制就成了多余的。

**风险三：证据基础薄弱**

支持 Harness Engineering 价值的大多数证据来自 AI 工具厂商自身——OpenAI 报告 Codex 有多厉害，Anthropic 报告 Claude Agent SDK 改进了多少。这些来源都存在利益冲突。独立的、定量的、可复现的 benchmark 验证目前仍然缺乏。Anthropic 自己都在文章里说了，他们的案例缺少显著的定量成功指标。

**风险四：可复现性缺口**

OpenAI 的百万行代码案例是在极其特定的条件下完成的：从空仓库开始，用自家的 Codex 工具，团队本身就是 AI 系统专家。这个经验对普通工程团队的可复现性完全没被验证过。

**风险五：Harness 也可能是风险放大器**

Anthropic 在 browse.com 的复盘中发现：多 Agent 配置并不一定改变模型想走捷径的倾向，但因为更高的 token 使用量和更多并行搜索者，反而提高了意外污染的概率。

| 配置 | 非预期解法发生率 |
| --- | --- |
| 单 Agent | 0.24% |
| 多 Agent | 0.87% |

Harness 不只是性能放大器，也可能是风险放大器。

---

## 最务实的行动路径

**立即能做的**：在项目根目录创建一个 agents.md，每次 Agent 犯重复性错误就加一条规则。

**中期投入的**：构建确定性验证层——linter、结构测试、pre-commit hooks，加上基本的可观测性。

**长期要做的**：设计模块化的、可替换的 Harness 架构，支持模型升级时平滑迁移。

---

## 一个开放性问题

如果 Harness 必须与模型能力边界相匹配，而模型能力在快速提升，那 Harness Engineering 作为一门工程学科，它的核心知识是会持续积累，还是会像很多补丁技术一样被下一代模型直接淘汰？

换句话说：**Harness 最终会成为 AI 时代的 DevOps，还是会成为另一个被遗忘的过渡概念？**

---

> **Agent 不难，Harness 才难。**
> — OpenAI Codex 团队工程师
`;

export default content;
