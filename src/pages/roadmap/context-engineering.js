const content = `# 上下文工程到底是什么？

> 面试官问你：Agent 开发里大家都在说的上下文工程到底是什么？
>
> 如果你只回答"就是把相关的信息塞进 prompt 里"，面试官大概率会追问：那这和 Prompt Engineering 有什么区别？任何调用大模型的应用不都在组装 prompt 吗？为什么 Agent 要单独拎出来叫一个工程？
>
> 说不清楚这个问题就丢分了。

---

## 先说最基本的一点

大模型在推理的时候，能用到的信息只来自两个地方：

1. **参数里的知识**：训练阶段学到的，推理阶段改不了
2. **上下文窗口里的内容**：你能控制的唯一部分

上下文工程本质上就是在**构建大模型的工作记忆**。你决定了它在做决策的那一刻能看到什么，你就决定了它的行为质量。

Anthropic 在官方博客里说得很直接：

> 构建 LLM 应用已经从"怎么找到正确的措辞"变成了回答一个更宏观的问题：**在模型生成的那一刻，什么样的上下文配置最可能产生期望的行为？**

---

## 和 Prompt Engineering 的核心区别

| | Prompt Engineering | 上下文工程 |
| --- | --- | --- |
| 关注点 | 怎么说（措辞、格式、few-shot 示例） | 看到什么（信息组成、结构、动态变化） |
| 适用场景 | 单轮问答 | 多步执行的 Agent |
| 核心问题 | 这句话怎么写更好？ | 这一轮推理时，窗口里该有什么、不该有什么？ |

对于单轮问答应用，两者差别不大。但对于 Agent，这个区别是巨大的。

**为什么？**

Agent 不是单轮的。一个 Agent 在执行任务的过程中，可能经过十几轮甚至几十轮的推理循环。Manus 团队分享的数据：一个典型任务平均要调用 **50 次工具**，每一轮都会产生工具调用的参数、返回的结果、模型的中间推理，这些信息会不断累积在上下文里。

---

## Agent 开发里最核心的矛盾

前沿模型的上下文窗口已经可以容纳百万 token，你可能觉得够用了。但 Manus 的经验是：**窗口大不等于问题解决了，反而可能是一种负担。**

随着上下文变长，模型的性能会明显下降——不是因为放不下，而是因为噪声太多，关键信息被淹没了。

Anthropic 给出了一个非常精炼的指导原则：

> 上下文工程的目标是找到**最小的高性价比的 token 集合**，最大化期望结果的概率。

注意：不是尽可能多塞信息，而是找到**最小的高质量集合**。这个思路是反直觉的，但这正是"工程"这个词的核心含义——精确的设计和权衡。

---

## 六层拆解：具体在工程什么

### 第一层：压缩重启（Compression）

这是 Anthropic 推荐的第一号技术，也是最基础的一道防线。

**做法**：当对话接近上下文窗口上限时，用模型自己对当前上下文做一次摘要总结，然后用这份摘要重新初始化一个新的上下文窗口。相当于模型写了一页笔记，然后翻开新的一页继续工作。

**关键**：选择保留什么、丢弃什么。Anthropic 的建议是先最大化召回，确保每一条重要信息都被捕获，然后再迭代提升精确度。

一个容易入手的优化：把已经完成的工具调用和原始返回结果清理掉，只保留结论。

Claude Code 就是这么做的——在长任务执行过程中会自动触发 compression，把之前的对话压缩成摘要，然后在新的上下文里继续工作，用户甚至感知不到这个过程。

---

### 第二层：外化记忆（Structured Note-taking）

有一类信息，它不属于当前这一轮对话，但 Agent 在后续步骤甚至后续会话中还需要用到。这类信息不能只靠压缩保留在上下文里，需要**写到外部**。

**做法**：让 Agent 在执行过程中把关键信息主动写到文件系统里。

- Claude Code 会创建一个 TODO list 来追踪任务进度
- Manus 会维护一个类似 notes.md 的文件来记录中间结论

Anthropic 举了一个很生动的例子：Claude 玩宝可梦的时候，要在数千个游戏步骤中保持连贯的策略，靠的就是在外部文件里维护精确的技能、训练进度、战斗策略以及探索区域的地图。这些信息不塞在上下文窗口里，而是在需要的时候从文件里读取。

> **更深层的洞察**：Manus 把文件系统当做终极的无限上下文。代码和数据保存在文件里，原始搜索结果存入文件而不是留在对话中，只保留结论和下一步操作。文件系统不是辅助工具，而是上下文架构的核心组成部分。

---

### 第三层：即时加载（Just-in-time Context）

传统 RAG 思路：在用户提问之后、模型回答之前，一次性把所有可能相关的信息检索出来塞进上下文。

但在 Agent 场景里，你没办法提前知道 Agent 在第十步会需要什么信息。

**Anthropic 推荐的做法**：Agent 不预加载所有数据，而是维护轻量级的标识符（文件路径、数据库查询、网页链接），需要的时候用工具动态加载数据到上下文中，用完就可以在下一次 compression 时清理掉。

Claude Code 就是这个模式的典型：分析大型代码库时，不会把所有代码一次性塞进上下文，而是用 grep 和 glob 命令及时搜索，按需读取。

> **本质**：上下文里放的不是信息本身，而是**获取信息的能力**。

---

### 第四层：上下文隔离（Multi-Agent Architecture）

当一个任务足够复杂时，即使做了压缩、外化记忆、即时加载，不同子任务的上下文也会互相干扰。

**解决方案**：用多 Agent 架构，主 Agent 负责规划和分配任务，每个子 Agent 拥有自己独立的、干净的上下文窗口。

Manus 的架构：
- **Planner**：负责拆分任务
- **Knowledge Manager**：审查对话并决定什么应该保存到文件系统
- **Executor Agent**：负责执行具体任务

每个子 Agent 的上下文窗口里只有它需要关注的信息，不会被其他子任务的细节污染。

Claude Code 也从单 Agent 演进到了多 Agent 架构——启动子 Agent 处理独立的子任务，子 Agent 完成后只把结果返回给主 Agent，中间过程不会污染主 Agent 的上下文。

> 可以把这理解为**分治法在上下文层面的应用**。

---

### 第五层：工具设计

这一层容易被忽略，但 Anthropic 明确指出，工具设计是上下文工程的核心组成部分。

**为什么？**

- 工具的定义本身就占据上下文空间
- 工具的返回结果是上下文膨胀的主要来源
- 工具集的大小直接影响模型的决策质量

Manus 的发现是反直觉的：**更多的工具可能让你的 Agent 变蠢**。随着可选工具增多，模型更可能选择次优的操作或者走低效路径。

Anthropic 也说了：如果一个人类工程师都无法确定在特定情况下该使用哪个工具，AI Agent 也不会做得更好。

**工程上的微妙之处**：你可能觉得那就动态增删工具，当前步骤不需要的工具就移除。但 Manus 发现不能这么做——在大多数 LLM 的实现里，工具定义位于上下文的前部，如果中途增删工具，后面所有内容的 KV cache 全部失效，需要重新计算，导致巨大的延迟和成本损失。

**Manus 的解决方案**：工具定义保持不变，但通过操作模型的 logits 来遮蔽当前不需要的工具。模型在解码时根本选不到被遮蔽的选项，但工具定义本身没有变动，KV cache 保持完整。

---

### 第六层：缓存友好架构

这是把上下文工程从"能跑"推向"能上生产"的关键一步。

Manus 团队说了一句非常重要的话：

> 如果只能选一个指标来衡量生产级 AI Agent 的质量，那就是 **KV cache 命中率**。它同时决定了延迟和成本。

**一笔具体的经济账**（以 Claude Sonnet 为例）：

| | 成本（每百万 token） |
| --- | --- |
| 不带缓存 | $3 |
| 启用 Prompt Caching | $0.3 |

省了 **90%**。

但 Manus 发现，比省钱更重要的是：围绕缓存来设计架构，会从根本上改变你思考上下文的方式。

**核心原则**：上下文的结构要尽量是追加式的。

\`\`\`
[系统指令 + 工具定义]  ← 固定前缀，每次复用
[历史对话轮次]         ← 追加，不修改
[本轮新内容]           ← 只计算这部分
\`\`\`

稳定的内容（系统指令、工具定义）放在上下文最前面，形成固定前缀，每一轮新产生的内容追加在后面。这样每次推理时，前缀部分的 KV cache 可以完全复用，只需要计算新追加的部分。

反过来，如果中途修改了前面的内容（比如动态增删工具、重写系统指令），前缀变了，后面所有内容的缓存全部作废——代价是灾难性的。

这就是为什么第五层说工具设计要用 masking 而不是增删：它不只是一个工程技巧，背后是整个缓存架构的约束。

---

## 两个容易被忽略的加分细节

**1. 保留错误信息**

直觉上你会觉得 Agent 执行过程中犯了错，应该把错误的步骤清理掉，给模型一个干净的上下文。但 Manus 的经验是相反的：**不要擦除失败**。

因为失败就是证据，模型看到之前的错误才能避免重复同样的错误。清理掉错误等于移除了学习信号。

**2. TODO 刷新（对抗注意力衰减）**

Manus 发现在长任务执行过程中，Agent 容易忘记自己的总体目标——不是因为信息丢了，而是因为目标被埋在了上下文的中间，模型的注意力没有对准它。这就是研究里说的 **Lost in the Middle** 现象。

解决方案很简单：在每一步结束后，让 Agent 重写它的 TODO list，把当前目标和剩余任务刷新到上下文的末尾。

这利用了模型对末尾内容注意力更强的特性，用自然语言偏置模型的注意力焦点，不需要任何架构改动。

---

## 面试答题框架

**先说本质**：上下文工程是在管理大模型推理时的工作记忆。和 Prompt Engineering 的区别在于：Prompt Engineering 关注指令怎么写，上下文工程关注的是每一次推理时上下文窗口里的信息组成和动态变化。目标是用最小的高信噪比的 token 集合，最大化期望结果的概率。

**再说为什么 Agent 特别需要它**：Agent 是多步执行的，上下文会随步骤不断膨胀。即使窗口够大，信息过多也会稀释注意力，降低性能。不做上下文工程的 Agent 跑不了复杂任务。

**然后说六层**：

| 层级 | 核心做法 |
| --- | --- |
| 压缩重启 | 接近窗口上限时，摘要压缩重启，第一道防线 |
| 外化记忆 | 关键信息写入文件系统，实现跨上下文持久记忆 |
| 即时加载 | 不预加载，维护轻量引用，需要时用工具拉取 |
| 上下文隔离 | 多 Agent 架构，每个子 Agent 拥有独立干净的上下文 |
| 工具设计 | 精简工具集，用 masking 代替动态增删，避免缓存失效 |
| 缓存友好架构 | 围绕 KV cache 命中率设计，稳定前缀 + 追加式更新 |

**最后补两个加分点**：保留错误信息作为学习信号；用 TODO 刷新把目标推到上下文末尾，对抗注意力衰减。
`;

export default content;
