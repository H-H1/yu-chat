const content = `# 为什么一个 Agent 不够用？

> 今天聊一个从 Claude Code 源码里扒出来的设计决策，它可能会改变你对 AI Agent 架构的理解。

---

## 你一定遇到过这些问题

用过任何一款 Coding Agent，你一定遇到过：

- 让他改一个 bug，他顺手把半个文件重构了
- 让他加一个功能，他给你加了三层抽象和五个你没要求的错误处理
- 让他加一行注释，他把整个文件的注释都重写了一遍

大部分人觉得这是模型的问题，模型不够聪明。

但从 Claude Code 的源码来看，Anthropic 不这么认为。**他们认为这是架构的问题。**

---

## Claude Code 内建了六个 Agent

这个系统里内建了至少六个不同的 Agent——不是一个万能 Agent 配上不同的 prompt，而是六个角色完全不同的 Agent，各有各的 system prompt，各有各的工具集，各有各的权限边界。

| Agent | 职责 |
| --- | --- |
| 通用任务 Agent | 负责日常的编码执行 |
| Explore Agent | 纯只读的代码探索专家 |
| Plan Agent | 只做规划，不执行任何操作 |
| Verification Agent | 专门做对抗性验证，工作就是找代码里的问题 |
| Guide Agent | 负责使用指导 |
| Status/Settings Agent | 负责状态栏配置 |

---

## 为什么要这么设计？

Anthropic 的出发点其实很朴素：**让一个 Agent 同时做研究、规划、实现、验证，每件事都做不扎实。**

这跟人类团队是一样的：

- 你让同一个人既写代码又做 code review，他天然倾向于觉得自己写的没问题
- 你让同一个人既做需求分析又做实现，他会不自觉地把需求往自己容易实现的方向解读

这不是能力问题，是**角色冲突**。

回到文章开头的那些痛点：

- 让 Agent 改一个 bug，他为什么会顺手重构？因为他在改 bug 的同时也在探索代码，探索过程中看到了可以优化的地方，于是就动手了
- 让他加功能，他为什么会加三层抽象？因为他同时在规划和实现，规划阶段觉得未来可能需要扩展，于是实现阶段就提前抽象了

**问题的根源是一个 Agent 承担了太多角色，导致每个角色的行为边界都模糊了。**

---

## 三个维度的隔离

Claude Code 的解法是把角色彻底拆开，而且不只是用不同的 prompt 来区分角色，而是在三个维度上做了隔离。

### 维度一：工具级隔离

Explore Agent 只能用 \`glob\`、\`grep\`、\`file_read\` 这些只读工具，连 bash 命令都只允许 \`git status\`、\`git log\` 这种不会改变任何东西的操作。

它被明确禁止：
- 创建新文件
- 修改已有文件
- 删除文件
- 用重定向写文件
- 运行任何改变系统状态的命令

为什么这么极端？因为探索阶段如果不小心改了东西，后面实现阶段就会出问题。**把权限彻底隔离是一种朴素但有效的安全设计。**

### 维度二：Prompt 隔离

每个 Agent 有自己的 system prompt，明确告诉他你是谁、你能做什么、你不能做什么。

其中 Verification Agent 的 prompt 写了 **130 行**，可能是整个源码里最精心设计的一段文本。它的核心方向就一个：**想办法搞坏它。**

不是温和地检查代码看起来对不对，而是像一个刻意找茬的 QA 工程师一样，主动去构造能让代码出错的场景，用实际运行的方式去证明代码有没有问题。

### 维度三：权限隔离

不同 Agent 有不同的权限模式，只读 Agent 在 runtime 层面就被锁死了。

**写权限不是靠 prompt 里写一句"你不要写文件"来约束**，而是在 runtime 层直接拦截。

三层隔离叠加在一起，才真正实现了角色分离。

---

## 按阶段分，而不是按功能分

这里有一个很关键的认知：很多人做多 Agent 系统，第一反应是按功能分——前端 Agent、后端 Agent、测试 Agent。

但 Claude Code 不是按功能分的，**它是按职责阶段分的**：探索 → 规划 → 实现 → 验证。

这两种分法有本质区别：

- **按功能分**：每个 Agent 内部还是要同时做研究、规划、实现、验证，冲突并没有解决
- **按阶段分**：每个 Agent 只负责一个阶段的职责，角色边界就清晰了

这跟传统软件工程里的流程设计是一样的道理——你不会让开发人员自己验收自己写的代码，不是因为不信任他的能力，而是因为角色冲突会导致系统性的偏差。

---

## 额外收益：成本优化

这种设计还带来了一个额外的好处：**成本优化**。

Explore Agent 在外部用户场景下，默认用 Haiku 模型——更快、更便宜。因为探索阶段不需要最强的推理能力，速度和成本更重要。而需要强推理的阶段（比如实现和验证）才采用更大的模型。

这跟 Harness Engineering 里提到的**推理三明治策略**是同一个思路：把算力花在刀刃上。

---

## 把行为规范写成制度

光拆角色还不够。Claude Code 还做了一件事：**把行为规范写成制度**。

它的 prompt 里有一个专门的函数，告诉模型什么该做、什么不该做：

- 不要加用户没要求的功能
- 不要过度抽象，三行重复代码好过一个不成熟的抽象
- 不要给你没改的代码加注释和文档字符串
- 不要做不必要的错误处理和兜底逻辑
- 不要设计面向未来的抽象
- 先读代码再改代码
- 不要轻易建新文件
- 方法失败了先诊断，不要盲目重试，也不要一次失败就放弃
- 结果要如实汇报，没跑过的不要说跑过了

每一条都是在解决日常使用 Coding Agent 时遇到的具体痛点。Anthropic 不是不知道模型会犯这些错误，他们的做法是**把"不要犯这些错误"写成制度性的约束，而不是指望模型每次都能自己想到**。

---

## 核心设计哲学：硬约束兜底，软约束补缝

> **不信任模型的自觉性，能用硬约束的地方绝不用软约束。**

- Explore Agent 不能写文件：不是靠 prompt 里写一句"请你不要写"，是工具集里压根儿没给他写文件的能力
- 风险操作要停下来确认：不是靠模型自己判断，是 runtime 层直接拦截
- 像"不要加用户没要求的功能"这种没法在代码层面拦截的行为，才退而求其次写进 prompt

**硬约束兜底，软约束补缝**，这比把所有希望都寄托在一段 prompt 上稳定得多。

---

## 总结

为什么一个 Agent 不够？

不是因为一个 Agent 能力不够，是因为一个 Agent 承担太多角色，角色之间会互相干扰：

- 探索阶段的好奇心会干扰实现阶段的克制
- 实现阶段的自信会干扰验证阶段的怀疑

**把角色拆开，给每个角色明确的权限边界和行为规范，是目前工业级 Agent 系统的标准做法。**

Claude Code 用 4756 个文件给出了一个非常完整的参考实现。

> 下一期：限制最极端的 Explore Agent（连创建文件都不被允许）和 prompt 最狠的 Verification Agent（130 行文本只干一件事：主动构造能让代码出错的场景）。
`;

export default content;
