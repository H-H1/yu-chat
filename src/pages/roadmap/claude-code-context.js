const content = `# Claude Code 上下文管理：源码级拆解

> Code 的上下文管理是整个 Harness 工程含量最高的部分。光 compact 目录下就有 15 个文件，加起来超过 15000 行。**上下文管理不是一个功能，它是一个完整的子系统。**

---

## 先看一个生产数据

源码注释里有一条来自 2026 年 3 月 10 日的生产数据：

> 1279 个会话出现了 50 次以上的连续自动压缩失败，最多的一个会话失败了 **3272 次**，每天浪费大约 **25 万次** API 调用。

修复方案是三行代码：连续失败超过三次就停止重试。

这个细节说明了什么？上下文管理不是一个设计好就完事的系统，它是一个在生产环境中持续踩坑、持续修补的战场。今天看到的每一个设计决策背后，都有真实的生产事故。

---

## 四层压缩架构

Claude Code 的上下文管理分成四层，按从轻到重的顺序：

\`\`\`
微压缩（零成本规则清理）
    ↓ 不够用
绘画记忆压缩（结构化事实提取）
    ↓ 不够用
完整压缩（模型调用摘要）
    ↓ 自动触发
熔断保护（防止失控）
\`\`\`

---

## 第一层：微压缩

### 核心逻辑

微压缩是成本最低的一层，**不调用模型，纯规则驱动**。

核心逻辑：按工具类型白名单，保留最近 N 个工具结果，把更早的结果清掉。

白名单工具类型：
- \`file_read\`、\`bash\`、\`glob\`、\`web_search\`、\`web_fetch\`
- \`file_edit\`、\`file_write\`

这些工具的输出通常很大，但时效性短。十分钟前读的文件内容，大概率已经不需要了。

### 两条清理路径

**路径一：Cached Micro Compact**

如果用户一直在连续对话，Anthropic API 的 prompt cache 还在（服务端会缓存 prompt 前缀，下次请求前缀一样就不用重新处理，省钱省时间）。

这条路径不修改本地消息内容，而是通过 cache editing API 告诉服务端把这几个工具结果删了，但不动 prompt 前缀，**缓存继续有效**。它维护一个全局状态，追踪所有已注册的工具结果，按 token 阈值决定哪些该删。

**路径二：Time-based Micro Compact**

如果用户离开了一段时间回来，服务端缓存早就过期了，没必要小心翼翼。系统计算距离上一条助手消息的时间间隔，超过配置阈值就直接改消息内容，把旧的工具输出替换成占位符。

### 主线程隔离

Cached Micro Compact 只对主线程生效。

原因：Claude Code 会 fork 出子 Agent 来做绘画记忆提取、prompt 建议等后台任务。如果这些子 Agent 把自己的工具结果注册到全局状态里，主线程下次尝试删除时，会去删一些在自己对话历史里根本不存在的工具。

源码里用 \`query_source\` 做了主线程隔离。

---

## 第二层：绘画记忆压缩

### 与微压缩的本质区别

- 微压缩：**删东西**
- 绘画记忆压缩：**提炼东西**

核心思路：不要把对话做摘要，而是从对话中提取**学到了什么**——项目结构、用户偏好、任务进度。这些结构化的事实被持久化到本地的记忆目录里。

### 保留多少最近消息

源码里有三个配置参数：

| 参数 | 默认值 |
| --- | --- |
| 最小 token 数 | 1 万 |
| 最小消息数 | 5 条 |
| 最大 token 数 | 4 万 |

系统从最后一条已总结的消息开始往前扩展，直到同时满足最小 token 数和最小消息数，或者触及最大 token 上限。

### 精细的边界处理

扩展时不能把 \`tool_use\` 和 \`tool_result\` 拆开：

- 如果保留了一条包含 \`tool_result\` 的用户消息，就必须同时保留包含对应 \`tool_use\` 的助手消息，否则 API 会报错。
- 还要处理 thinking block 的情况：流式传输时，同一个 message ID 可能被拆成多条消息，thinking 在一条，\`tool_use\` 在另一条，不能只保留后者。

### 优势

不需要调用模型来做摘要，直接用已经提取好的结构化记忆，**成本比完整压缩低得多**。而且保留了最近的原始消息，模型可以看到最近几轮对话的完整细节。

---

## 第三层：完整压缩

当绘画记忆压缩不可用或不够用时，系统回退到完整压缩。**这一层需要调用模型。**

### Prompt 设计

源码里的 prompt 要求模型按九个维度总结对话：

1. 用户的请求和意图
2. 关键技术概念
3. 文件和代码片段
4. 错误和修复
5. 问题解决过程
6. 所有用户消息
7. 待办任务
8. 当前工作
9. 可选的下一步

### Analysis 机制（内嵌 CoT）

最精妙的设计：prompt 要求模型先在 \`<analysis>\` 标签里做详细分析，然后再在 \`<summary>\` 标签里输出最终摘要。

\`<analysis>\` 部分在格式化时会被**完全剥离**，不会进入压缩后的上下文。它的作用纯粹是让模型在输出摘要之前先想清楚，提高摘要质量。

> 这本质上就是一个内嵌的 Chain of Thought。

### 压缩请求本身超长怎么办

压缩请求本身可能触发 \`prompt_too_long\` 错误——用来缩短上下文的操作，输入本身就超长了。

源码里的解法：按 API 轮次分组，从最早的组开始丢弃，最多重试三次。

### 压缩后的善后工作

压缩完成后，系统还要重新注入被丢掉的上下文：

- 清空文件读取缓存，重新注入最近访问的文件内容（最多 5 个文件，总预算 5 万 token）
- 重新注入 plan 文件、skill 内容、MCP 工具说明、agent 列表

---

## 第四层：自动触发与熔断

### 触发时机

\`auto_compact\` 系统在每次模型调用之后检查当前 token 用量。如果超过阈值（上下文窗口 - 13000 token 的缓冲），自动触发压缩。

触发顺序：
\`\`\`
先尝试绘画记忆压缩
    ↓ 不可用或压缩后仍超标
回退到完整压缩
\`\`\`

### 熔断器设计

连续失败超过三次就停止重试，避免在上下文不可恢复的情况下反复浪费 API 调用。

这就是开头提到的那个 **25 万次 API 调用**的修复。

### 排除逻辑

\`session_memory\` 和 \`compact\` 这两个 query source 的请求不会触发自动压缩。

原因：它们本身就是 fork 出来的子 Agent，如果它们也触发压缩，会死锁。

---

## 工具系统也是上下文工程

Claude Code 内置了 80 多个工具，但不是一开始就全部塞进上下文：

- **核心工具**：启动时加载
- **扩展工具**：通过 \`tool_search\` 按需发现和加载
- 每个工具用 Zod 的 schema 定义输入参数，模型输出的 JSON 必须通过验证才能执行

### 大输出的处理

如果工具输出太大，系统不会直接截断，而是存到外部的 \`tool_result_storage\`，给模型一个摘要加一个指针，模型按需取用，而不是被迫消化一整块巨大的输出。

### Snip Compact

一个叫 \`snip_compact\` 的机制，可以在不做完整压缩的情况下，从对话历史中剪掉特定的消息段，释放 token 空间。这是比完整压缩更轻量的中间方案。

---

## 总结

回过头看整个上下文管理系统：

| 层级 | 方式 | 成本 | 适用场景 |
| --- | --- | --- | --- |
| 微压缩 | 规则清理 | 零成本 | 工具输出过多 |
| 绘画记忆压缩 | 结构化事实提取 | 低成本 | 有持久化记忆时 |
| 完整压缩 | 模型调用摘要 | 高成本 | 前两层不够用 |
| 自动触发 | 熔断保护 | — | token 超阈值 |

每一层都假设上一层可能不够用，每一层都有自己的成本和适用场景。

这套系统最让人印象深刻的不是某一个单独的设计，而是它对**"上下文是稀缺资源"**这个认知的彻底贯彻：

- 微压缩区分缓存是否有效来决定清理策略
- 绘画记忆压缩精确计算保留多少条消息
- 完整压缩用 analysis 标签提高摘要质量
- 自动触发的熔断器防止失控

每一个决策都在问同一个问题：**这个 token 值不值得留在上下文里？**

> Agent 的上下文窗口不是一个被动的容器，是一个需要主动管理的稀缺资源。管理得好，Agent 可以在长达数小时的会话中保持连贯性。管理得不好，25 万次 API 调用就这么烧掉了。
`;

export default content;
