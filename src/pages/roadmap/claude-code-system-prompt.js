export default `# Claude Code 系统提示词模块化

## 静态段与动态段分离

Claude Code 的系统提示词通过边界标记分为两半。

**前半段为静态内容**，包括身份介绍、安全指令、代码风格规则、工具使用指南、行动风险评估框架等，对所有用户和会话通用。

**后半段为动态内容**，包含当前会话启用的工具、记忆系统摘要、工作目录、操作系统信息、用户语言偏好、外部插件使用说明等，因会话而异。

这种分离设计利用大模型 API 的提示词缓存机制，通用静态内容可跨用户共享缓存，提高缓存命中率，降低成本。

---

## 代码风格指令的具体化

区别于模糊的"写高质量代码"，Claude Code 的代码风格指令为具体可执行规则：

- 不对未修改代码添加注释文档、字母串或类型标注，仅在逻辑不明确处注释
- 不为假设的未来需求设计
- 不创建一次性操作的工具函数，三行相似代码优于过早抽象
- 不添加未要求的功能，bug 修复不顺便清理周围代码，简单功能无需额外可配置性
- 不针对不可能场景添加错误处理、降级方案或校验逻辑，仅在系统边界做校验

> 规则越具体，模型越明确行为边界。

---

## 行动风险评估框架

该框架通过**可逆性**和**影响范围**两个维度指导模型判断操作是否需要确认。

| 操作类型 | 示例 | 策略 |
|---|---|---|
| 本地可逆操作 | 编辑文件、跑测试 | 自由执行 |
| 不可逆 + 影响共享系统 | 强制推送、删除分支、发送消息、修改基础设施权限 | 必须先确认 |

**需确认的具体操作包括：**

- 删除文件 / 分支 / 数据库表
- 强制推送、修改已发布提交
- 发送消息到外部服务
- 上传内容到第三方工具

**遇到意外状态的处理原则：**

- 遇到不认识的文件 / 分支 / 配置 → 先调查再行动，不直接删除或覆盖
- 遇到锁定文件 → 先查进程持有情况，不直接删除

此框架培养模型自主判断能力，而非依赖固定规则列表。

---

## 安全指令的专业维护机制

系统提示词中 \`cyber risk instruction\` 安全指令定义模型在安全相关请求的行为边界：

**允许协助的场景：**
- 授权安全测试
- 防御性安全
- CTF 挑战
- 教育场景

**拒绝的场景：**
- 破坏性技术
- 拒绝服务攻击
- 大规模目标攻击
- 供应链攻击
- 恶意目的的检测规避

源码注释明确该指令由专人负责，任何修改需经安全团队审批评估，因修改会显著影响模型处理安全请求的方式。

这体现系统提示词作为 agent 行为规范的严肃性——关键段落需明确负责人和审批流程，类似关键代码的 code review。

---

## 防止模型说谎的双向约束

**防止虚假声明（正向约束）：**

- 测试失败需说明并附上输出
- 未执行验证步骤需如实告知
- 禁止在输出失败时声称测试通过
- 禁止压缩 / 简化失败检查制造虚假成功
- 禁止将未完成 / 有问题工作说成已完成

**防止过度谨慎（反向约束）：**

- 检查通过或任务完成时直接说明
- 不加不必要免责声明
- 不将已完成工作降级为部分完成
- 不重复验证已检查内容

目标是**准确报告**而非**防御性报告**。

此设计针对特定模型版本近 30% 的虚假声明率，通过双向约束对抗模型讨好用户的默认倾向。

---

## 系统提示词设计三原则

**1. 模块化**

将不同职责内容拆分为独立段落（身份、规则、工具指南、环境信息），便于独立修改和动态组合。

**2. 具体化**

每条规则需具体到模型可直接执行，如"不要给未修改代码加注释"而非"写好代码"。

**3. 分离缓存**

通用内容放前，会话特有内容放后，提升 API 缓存效率，降低成本。

---

> 系统提示词是 agent 的操作系统，定义其身份、能力边界、行为规范和风险判断框架。精确性决定 agent 行为的可控性。
`;
