const content = `# Agent 工程师的核心能力

> 今天聊一个比任何具体框架都重要的问题：**Agent 工程师的核心能力到底是什么？**

---

## 为什么"学这些很快过时"这句话有道理，但不完全对

每次发一个新的 Agent 概念视频，评论区一定会有人说：*"学这些很快过时了。"*

背后有两个原因：

**原因一：框架迭代太快**

LangChain 从 0.1 到 0.3，接口全改了。AutoGen 的多 Agent 写法换了好几轮，LlamaIndex Agent 模块重构了两三次。如果你学的是怎么调某个框架的某个类某个方法，那确实半年就过期。你今天背的 API 签名，下个版本可能连类名都不存在了。

**原因二：模型能力的进化本身在不断吞噬工程代码**

- 上下文窗口只有 4000 token 时，你必须做分块检索拼接，写一整套 RAG 流水线。窗口变成 128K 之后，很多场景这套流水线不需要了。
- 模型不会用工具时，你得写复杂提示词教它输出特定格式，再用正则解析。模型原生支持 Tool Use 之后，这些代码可以直接删掉。
- 模型推理能力变强后，很多以前需要 Chain of Thought 手动引导的场景，现在一个 prompt 就能搞定。

所以有人下结论：**Agent 开发就是给模型打补丁，模型越强补丁越少，最后工程师就没活干了。**

---

## 两类完全不同的工程

这两个原因都指向同一个焦虑：*我现在学的东西保质期到底有多长？*

我的回答是：**取决于你学的到底是什么。**

Agent 开发里有两类完全不同的工程：

### 补偿性工程（会过时）

本质是用外部代码弥补模型能力的不足：

- 窗口太小 → 做分块检索
- 模型不会调工具 → 写输出解析器
- 模型容易跑偏 → 手写 Chain of Thought 模板

这类工程确实会过时，模型能力一提升，对应的代码就可以直接删掉。

### 系统性工程（不会消失）

解决的不是模型不够强的问题，而是**任何智能体在真实环境中工作都必须面对的问题**。

从 2023 年初的 ReAct 模式，到 2023 年底的 Plan & Execute，再到 2024 年的 Multi-Agent，2025 年的 Context Engineering，再到现在的 Harness Engineering。名词换了五六轮，但每一次所谓的"范式切换"解决的核心问题从来没变过。

---

## 核心结论：四项不变的能力

> Agent 工程师的核心能力就四件事：**上下文管理、控制流设计、错误恢复、反馈回路。**

这四个词似曾相识：

| Agent 能力 | 传统软件工程对应 |
| --- | --- |
| 上下文管理 | 状态管理 |
| 控制流设计 | 流程编排 |
| 错误恢复 | 异常处理 |
| 反馈回路 | 监控告警 |

Agent 开发没有发明新的工程学科，它站在传统软件工程的地基上。但多了一层全新的挑战：**你的核心引擎不再是你亲手写出来的确定性逻辑，而是一个概率性的黑盒模型。**

就这一个差异，把同样的四件事变成了完全不同的工程。

---

## 前提对齐：Model + Harness

行业逐渐形成了一个共识：

\`\`\`
Agent = Model + Harness
\`\`\`

- **Model**：负责智能推理决策
- **Harness**：负责模型之外的一切——工具、知识、上下文管理、权限边界

模型是引擎，Harness 是方向盘和刹车。模型做决策，Harness 提供执行环境。

**我们 Agent 工程师的工作就是造车。** 发动机不是我们造的（那是 Anthropic、OpenAI、Google 的事），我们造的是底盘、方向盘、刹车系统、仪表盘和安全气囊。

---

## 第一项：上下文管理

### 传统开发 vs Agent 开发

传统后端的状态管理：Session 里存用户信息，Redis 里存缓存，数据库里存业务数据。你从来不需要担心一件事——你存进去的数据不会影响你的业务逻辑正不正确。逻辑是你写死的，数据是数据，逻辑是逻辑，泾渭分明。

**Agent 开发里这个前提不成立了。**

Agent 没有你手写的 if-else，它的全部逻辑都是模型在推理时基于上下文生成的。你喂给它什么信息、喂的顺序、措辞、信息量的多少，直接决定它的推理质量和行为方向。

> **上下文不是数据，上下文就是程序本身。**

### 三个层次的问题

**层次一：上下文隔离**

把大任务拆成多个子任务时，每个子任务应该有自己独立的上下文。把主任务的所有历史消息带进子任务，模型会被无关信息干扰，推理质量下降。

子任务的独立上下文本质上是一道防火墙，防止一个子任务的噪声污染另一个子任务的推理。这也是为什么 Claude Code 的子代理模式、OpenAI Codex 的沙箱执行都在做同一件事。

**层次二：按需注入**

不要把所有知识一股脑塞进 system prompt。模型需要什么信息，在它需要的时候再给它。上下文窗口是稀缺资源，塞太多无关信息等于往模型的工作台上堆满杂物，它反而找不到真正有用的东西。

**层次三：上下文压缩**

Agent 持续运行，上下文会不断膨胀，但模型的推理能力会随着上下文膨胀而退化。你的数据库不会因为存的数据太多而算错，但模型会。所以你需要压缩策略，在保留关键信息的前提下，定期为上下文瘦身。

> 从 2023 年 ReAct 时代手写 prompt 模板安排信息顺序，到 2025 年正式提出 Context Engineering，行业花了三年才给这件事正式命名，但底下的事情一直是同一件事。

---

## 第二项：控制流设计

### 传统开发 vs Agent 开发

传统后端的流程编排：用户下单 → 校验库存 → 扣减库存 → 创建订单 → 发送通知。每一步做什么，下一步走哪个分支，全由你的代码决定。

**Agent 开发里下一步做什么不是你决定的，是模型决定的。**

所有 Agent 框架的核心都是同一个循环：调模型 → 模型说要调工具就调工具 → 把结果喂回去继续循环 → 模型说停就停。循环本身没有任何业务逻辑，模型自己决定什么时候调用工具、调用哪个工具、什么时候结束。

### 你需要设计更精巧的控制结构

**计划机制**：一个没有计划的 Agent 会漫无目的地漂移。你不写死流程，但你给模型一个"先列计划再执行"的工具，引导它在行动前先想清楚步骤。实测表明，加了计划机制的 Agent 任务完成率几乎翻倍。

**任务依赖图**：你不写死执行顺序，但你提供一个结构，让模型自己把大目标拆成有依赖关系的小任务，按依赖关系逐步推进，完成一个才解锁下一个。

**自主认领机制**：在多 Agent 协作场景里，你不指派任务，而是设计一个看板，Agent 自己扫描、自己认领、自己执行。

> **传统控制流是你写死的 A → B → C。Agent 的控制流是你设计一个棋盘，模型在棋盘内自主决策。你不控制每一步怎么走，你控制它能走的范围。**

这需要一种完全不同的设计思维：从命令式转向**声明式 + 约束式**。约束解空间反而提高了产出。

---

## 第三项：错误恢复

### 传统开发 vs Agent 开发

传统后端的异常处理：try-catch、事务回滚、重试、降级、熔断。它有一个隐含的前提假设——逻辑是对的，只是执行可能失败。网络超时、数据库挂了、下游返回 500，这些都是环境故障，不是逻辑错误。你的代码本身不会犯错。

**Agent 开发里这个假设也不成立了。**

模型会误解意图、调错工具、传错参数，甚至产生幻觉，声称已经完成了实际没完成的操作。这不是 bug，不是异常，这是概率系统的本性——犯错是它工作方式的一部分。

### 两层问题

- **传统环境故障**：API 超时、文件不存在（和以前一样处理）
- **智能故障**：模型推理本身出错（全新问题）

**核心原则：在架构层面隔离错误，不让它级联传播。**

子任务用独立上下文执行，失败了，它的错误推理轨迹不会污染主任务。主任务只拿到最终结果，然后决定下一步。这和微服务架构里的故障隔离原理完全一致。

> OpenAI Codex 团队分享过一个经验：**当 Agent 出错的时候，解决方案几乎从来不是让它再试一次，而是去思考模型缺了什么能力或者什么信息，然后把那个东西补上。**

传统开发里 retry 是合理策略，因为错误来自环境波动，重试可能就好了。但 Agent 的智能故障不是随机波动，是结构性缺陷。模型缺了关键信息，你让它试 100 次，它 100 次都会犯同样的错。正确的做法是找到那个缺失的信息或能力，把它补进 Harness 里。

---

## 第四项：反馈回路

### 传统开发 vs Agent 开发

传统后端的监控告警：你监控的是系统运行状态，QPS、延迟、错误率。这些数据是给人类工程师看的，看到异常后人工介入处理。

**Agent 开发里反馈不是给人看的，反馈是给模型看的。**

**计划跟踪**：Agent 执行过程中，系统不断把"你的计划列表里还有哪些没做完"注入到它的上下文里，防止它跑偏。这不是给人看的 dashboard，这是给模型看的实时仪表盘。

**后台任务通知**：耗时操作放到后台异步执行，执行完毕后结果通过通知机制注入 Agent 的上下文，Agent 据此决定下一步行动。

**自验证循环**：在 Agent 准备退出时拦截它，强制执行一轮验证。推理三明治策略：规划阶段用最高推理强度理解问题，执行阶段降低强度保证速度，验证阶段再拉回最高强度捕获错误。

> **反馈回路的本质是让执行结果实时回流到模型的上下文中，驱动它自我评估和修正。不等人来看，不等人来修，系统自己形成闭环。**

---

## 为什么这四件事不会被模型吞噬

有人会说：你讲的这些迟早会被模型本身内化掉。

这个论点有一个根本性的盲区。类比一下：

- CPU 性能提升了几百万倍，但我们并没有因此不再需要操作系统
- 数据库引擎越来越强大，但我们并没有因此不再需要数据库设计
- 编译器优化越来越智能，但我们并没有因此不再需要软件架构

**为什么？因为每一次底层能力的提升，都会淘汰一批补偿性的工程实践，同时让系统性的工程实践变得更重要，而不是更不重要。**

底层能力越强，你能用它构建的系统就越复杂，而越复杂的系统越需要精心的工程设计。

就算模型的上下文窗口变成无限大，你仍然需要上下文管理。因为问题从来不是装不装得下，而是**该不该装进去**。窗口从 4000 变成 128K，你的工程问题不是消失了，是从"怎么把信息塞进去"变成了"怎么在海量可用信息中筛选出此刻真正相关的那一小部分"。

---

## 最终总结

> **Agent 工程师的核心能力是：在不确定性之上构建确定性。**

你的核心引擎是概率性的，它会犯错、会幻觉、会跑偏。你的工程任务是在它周围建造一个系统，让最终交付的结果是可靠的、可预期的、可恢复的。

| 能力 | 作用 |
| --- | --- |
| **上下文管理** | 让模型在每个决策瞬间看到正确的、充分的、不过载的信息 |
| **控制流设计** | 给模型一个结构化的棋盘，让它自主推进 |
| **错误恢复** | 假设模型一定会犯错，在架构层面隔离和修复 |
| **反馈回路** | 让执行结果实时回流，驱动模型自我修正 |

这四件事从 ReAct 到 Harness Engineering 从未改变。框架会过时，API 会迭代，但**如何让一个概率系统可靠地完成工作，这个问题不会过时。**

把时间花在不变的东西上，这是最好的建议。
`;

export default content;
